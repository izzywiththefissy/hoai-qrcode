# hoai-qrcode
This repository is a static webpage for generating a QR Code from a URL or one line of text
There is a box for imputing the text and the generated QR code is shown below. A button, 
underneath lets you downloa dthe code as a PNG image. This code is generated by an AI agent,
as an example of how to use it.

Testing
=======

Serve this folder with a simple HTTP server an dopen the page for testing.
This repository is the deployed as a static webpage using the default GitHub pages
workflow.
